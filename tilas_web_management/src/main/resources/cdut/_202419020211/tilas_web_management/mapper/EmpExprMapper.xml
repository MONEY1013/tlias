<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cdut._202419020211.tilas_web_management.mapper.EmpExprMapper">



    <!-- 批量保存员工工作经历 -->
    <insert id="batchInsert">
        insert into emp_work_experience (emp_id, company, position, start_date, end_date)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.empId}, #{item.company}, #{item.exprJob}, #{item.entryDate}, #{item.endDate})
        </foreach>
    </insert>

    <!-- 保存单条员工工作经历 -->
    <insert id="saveEmpExpr">
            insert into emp_work_experience (emp_id, company, position, start_date, end_date)
            values (#{empId}, #{company}, #{exprJob}, #{entryDate}, #{endDate})
    </insert>



    <!-- 更新员工工作经历 -->
    <update id="updateExpr">
            update emp_work_experience
            set company = #{company},
            position = #{exprJob},
            start_date = #{entryDate},
            end_date = #{endDate}
            where id = #{id}
    </update>

</mapper>
